services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    working_dir: /data
    volumes:
      - ./data:/data
    ports:
      - "8100:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "8180:80"
    depends_on:
      - backend
    restart: unless-stopped
