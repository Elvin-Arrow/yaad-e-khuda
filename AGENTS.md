## Agent skills

### Issue tracker

Issues are tracked in this repo's GitHub Issues (`Elvin-Arrow/yaad-e-khuda`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (one `CONTEXT.md` + `docs/adr/` at the repo root, created lazily). See `docs/agents/domain.md`.
